type Query {
    components: [Component]
}

type Component {
    id: ID!
    name: String
    amount: Float
}

extend type Mutation {
    addComponent(name: String, amount: Float, fluidId: String): Fluid!
}