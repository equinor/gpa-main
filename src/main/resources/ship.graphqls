type Ship {
    id: ID!
    name: String!
    country: String!
}

input ShipInput {
    name: String!
    country: String!
}

extend type Query {
    ships: [Ship]
}

extend type Mutation {
    newShip(
        name: String,
        country: String
    ):Ship!
}