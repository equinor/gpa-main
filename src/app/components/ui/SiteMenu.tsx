import './SiteMenu.css';

import React from "react";

export interface IProps { }

export interface IState { }

export class SiteMenu extends React.Component<IProps, IState> {
  constructor(props: IProps) {
    super(props);
    this.state = {
    };
  }

  render() {
    return (
      <div className="SiteMenu">
        .
      </div>
    );
  }
};
