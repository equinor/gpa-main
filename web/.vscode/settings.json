{
    "files.exclude": {
        "**/*.css": {
            "when": "$(basename).scss"
        },
        "**/*.css.map": {
            "when": "$(basename)"
        }
    },
    "editor.gotoLocation.multiple": "goto"
}